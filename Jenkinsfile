pipeline {
  agent any
  stages {
    stage('initialize') {
      steps {
        echo 'Getting started'
      }
    }
    stage('first') {
      steps {
        echo 'Getting started'
      }
    }
  }
}
